import BASE_URL from "../../../../../core/data/remote/base"

const URL = `${BASE_URL}/proposal`

const ProposalEndpoints = {
    extractList: `${URL}/xxx`,
    addEntry: `${URL}/xxx`
}

export default ProposalEndpoints